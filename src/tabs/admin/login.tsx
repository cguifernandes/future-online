import Form from "../forms/form-login";

const Admin = () => {
	return (
		<main className="w-full min-h-screen flex items-center justify-center">
			<div className="bg-black/70 p-5 rounded-lg">
				<Form />
			</div>
		</main>
	);
};

export default Admin;
